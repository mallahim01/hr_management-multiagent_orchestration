# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
tabulate>=0.9.0
rich>=13.0.0
flask>=3.0.0

# Knowledge base — hybrid RAG over Milvus with Google embeddings.
# google-genai supplies the embedding client (chat generation stays on Groq).
pymilvus>=2.5
google-genai>=1.0.0
pypdf>=4.0.0                  # optional: lets HR upload PDF policy documents

# LangGraph (real StateGraph orchestration + ASCII/Mermaid graph export)
langgraph>=0.0.40
grandalf>=0.8

# CrewAI (real Agent/Task/Crew orchestration)
# The litellm extra is required for any provider CrewAI does not resolve
# natively — including Groq, which is this project's default.
crewai[litellm]>=0.28.0

# Google ADK (real Agent + tool-dispatch orchestration)
google-adk>=1.0.0
