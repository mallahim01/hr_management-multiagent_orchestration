# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
tabulate>=0.9.0
rich>=13.0.0
flask>=3.0.0

# LangGraph (real StateGraph orchestration + graph image export)
langgraph>=0.0.40
grandalf>=0.8

# Visualization (graph rendering)
matplotlib>=3.7.0
networkx>=3.0

# Optional: Install if you want to use real CrewAI backend
# crewai>=0.28.0
