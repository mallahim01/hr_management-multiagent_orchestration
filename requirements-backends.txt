# Optional orchestration backends.
#
# Both are full integrations against the real SDKs. They are kept out of the
# default install only because they pull in roughly 2 GB of transitive
# dependencies between them; the native and langgraph backends need nothing here.
#
#     pip install -r requirements.txt -r requirements-backends.txt

# CrewAI (real Agent/Task/Crew orchestration).
# The litellm extra is required for any provider CrewAI does not resolve
# natively — including Groq, which is this project's default.
crewai[litellm]>=0.28.0

# Google ADK (real Agent + tool-dispatch orchestration).
# Also needs GOOGLE_API_KEY and a current Gemini model (see ADK_MODEL).
google-adk>=1.0.0
